imports:
    - { resource: ../common/config.yml }
    - { resource: parameters.yml }

framework:
    test: ~

swiftmailer:
    disable_delivery: true

security:
    encoders:
        FOS\UserBundle\Model\UserInterface: plaintext
        TorrentBundle\Entity\User: plaintext
